# Core API stack
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Chinese typo/error correction (pulls in torch + transformers).
# MacBertCorrector is imported lazily, so the rest of the service (sensitive
# detection, pipeline orchestration, tests) works without these installed.
pycorrector>=0.4.7
# The baked-in model is stored as safetensors; ensure the loader is present.
safetensors>=0.4
